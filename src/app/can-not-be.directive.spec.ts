import { CanNotBeDirective } from './can-not-be.directive';

describe('CanNotBeDirective', () => {
  it('should create an instance', () => {
    const directive = new CanNotBeDirective();
    expect(directive).toBeTruthy();
  });
});
